import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Chef = ({singleChef}) => {
    
    return (
        <div>
            <h2>This is chef</h2>
        </div>
    );
};

export default Chef;